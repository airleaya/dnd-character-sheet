# Release Notes - v0.14.21

> Historical damaged draft

This file is preserved only as a pointer to the old `v0.14.21` release-note attempt. The previous contents were mojibake and should not be treated as a reliable source of project history, feature scope, verification results, or release wording.

## Status

- Historical draft: damaged / not authoritative.
- Current source of truth for completed work: `UPDATE_LOG.md`.
- Current source of truth for active and pending work: `TODOLIST.md`.
- Current subsystem index: `PROJECT_INDEX.md` and `docs/index/`.

## Replacement Policy

If release notes are needed again, regenerate them from `UPDATE_LOG.md` instead of editing or restoring the damaged text. For `0.15.x` and later, release notes should summarize the relevant version entries and cite the verification commands already recorded in the update log.

## Related Records

- `UPDATE_LOG.md` contains the detailed `0.14.21` entry and surrounding version history.
- `TODOLIST.md` tracks the current baseline and unresolved documentation cleanup.
- `docs/index/documents-and-workflow.md` defines which documents are authoritative.
